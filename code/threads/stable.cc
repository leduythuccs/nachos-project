#include "stable.h"

STable::STable() {}

STable::~STable() {}

int STable::Create(char* name, int init) { return 0; }

int STable::Wait(char* name) { return 0; }

int STable::Signal(char* name) { return 0; }

int STable::FindFreeSlot(int id) { return 0; }
